# == Schema Information
#
# Table name: memberships
#
#  id         :uuid             not null, primary key
#  group_id   :uuid             not null
#  actor_type :string           not null
#  actor_id   :uuid             not null
#  level      :string
#  created_at :datetime         not null
#  updated_at :datetime         not null
#

darth_vader_board_of_directors:
  group: board_of_directors
  actor: darth_vader (Person)
  level: MyString

luke_skywalker_supporter_of_the_rebels:
  group: supporter_of_the_rebels
  actor: luke_skywalker (Person)
  level: MyString
